l = int(input('Digite a largura: '))
a = int(input('Digite a altura: '))
linha = 1
coluna = 1
while linha <= a:
    
    while coluna <= l:
        print('#', end= '')
        coluna = coluna + 1
    linha = linha + 1
    coluna = 1
    print()